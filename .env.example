# Server Configuration
NODE_ENV=development
PORT=3001
BASE_URL=http://localhost:3001

# PDF Service Configuration
PDF_ASSESSMENT_DIR=public/assessments
LOG_LEVEL=debug

# Security Configuration
# Comma-separated list of allowed origins (leave empty to allow all)
ALLOWED_ORIGINS=https://your-main-app.com,https://another-allowed-origin.com
# Comma-separated list of allowed IP addresses (leave empty to allow all)
ALLOWED_IPS=192.168.1.100,10.0.0.5
